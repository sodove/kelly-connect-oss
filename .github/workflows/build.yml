name: Build

on:
  push:
    branches: [ master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master ]

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Compile protocol module
        run: ./gradlew :protocol:compileKotlinJvm

      - name: Compile Desktop (JVM)
        run: ./gradlew :composeApp:compileKotlinDesktop

      - name: Compile Android
        run: ./gradlew :composeApp:compileDebugKotlinAndroid

  android-apk:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        run: ./gradlew :composeApp:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: kelly-connect-debug
          path: composeApp/build/outputs/apk/debug/*.apk
          retention-days: 14

  desktop-jar:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Package Desktop uber-jar
        run: ./gradlew :composeApp:packageUberJarForCurrentOS

      - name: Upload Desktop jar
        uses: actions/upload-artifact@v4
        with:
          name: kelly-connect-desktop
          path: composeApp/build/compose/jars/*.jar
          retention-days: 14

  release:
    runs-on: ubuntu-latest
    needs: [ android-apk, desktop-jar ]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: kelly-connect-debug
          path: artifacts/apk

      - name: Download Desktop artifact
        uses: actions/download-artifact@v4
        with:
          name: kelly-connect-desktop
          path: artifacts/desktop

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/apk/*.apk
            artifacts/desktop/*.jar
